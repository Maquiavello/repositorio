# Database - Credenciales de PostgreSQL
DB_NAME=app_db               # Nombre de la base de datos que creaste
DB_USER=postgres             # Usuario de PostgreSQL
DB_PASSWORD=mysecretpassword # Contraseña que usaste en Docker
DB_HOST=localhost            # Dirección donde está PostgreSQL
DB_PORT=5432                 # Puerto por defecto de PostgreSQL

# Django - Configuración de la aplicación
SECRET_KEY=tu-clave-secreta-generada-aqui-mas-segura  # Clave más segura
DEBUG=True                   # True para desarrollo, False para producción

# Para producción (Render) - URL completa de la base de datos
DATABASE_URL=postgresql://postgres:mysecretpassword@localhost:5432/app_db
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"